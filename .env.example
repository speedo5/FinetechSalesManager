# =============================================================================
# FINETECH POS - FRONTEND ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update the values for your environment
# NEVER commit .env to version control - only .env.example
# =============================================================================

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------
# Backend API base URL (include /api suffix)
# Local development: http://localhost:5000/api
# Production: https://your-production-api.com/api
VITE_API_URL=http://localhost:5000/api

# -----------------------------------------------------------------------------
# FEATURE FLAGS (Optional)
# -----------------------------------------------------------------------------
# Set to 'true' to use mock data instead of real API (for UI development)
# When false or not set, the app will use real API calls
VITE_USE_MOCK_DATA=false

# Enable debug mode for additional console logging
VITE_DEBUG_MODE=false

# -----------------------------------------------------------------------------
# APP CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# Application name displayed in UI
VITE_APP_NAME=Finetech POS

# Company name for receipts and reports
VITE_COMPANY_NAME=Finetech Media Ltd

# Support email
VITE_SUPPORT_EMAIL=support@finetech.co.ke

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - All VITE_ prefixed variables are exposed to the browser
# - Do NOT store sensitive secrets (passwords, API keys) in frontend .env
# - Use backend environment variables for sensitive data
# - After changing .env, restart the dev server (npm run dev)
